---
- name: Install GUI apps from official repos
  become: true
  community.general.pacman:
    name: "{{ apps_packages }}"
    state: present
    update_cache: true
  when: apps_packages | default([]) | length > 0
